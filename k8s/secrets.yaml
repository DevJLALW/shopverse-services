apiVersion: v1
kind: Secret
metadata:
  name: shopverse-secrets
  namespace: shopverse
type: Opaque
data:
  # Base64 encoded values (local development only - not for production)
  # These are example credentials, change as needed
  # echo -n "admin" | base64 -> YWRtaW4=
  # echo -n "admin123" | base64 -> YWRtaW4xMjM=
  redis-password: ""  # Empty password for local Redis
